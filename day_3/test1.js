getConnection = function(url, callback){
	...some code
	callback(con);
};

query = function(con, callback) {
	...som code
	callback(rows);
}

servic = function(as, callback){
	query(asd, cal)
}

route("/get/user/1", servic(function(quer, function(getConn)))))


getConnection = promise();

getConnection.then(que().the).catch()
